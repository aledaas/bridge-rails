# aledaas/bridge-rails

Bridge.xyz rails driver for Laravel (API client + webhook verification).

## Install
This package is typically consumed via a monorepo `path` repository during development.

## Env
BRIDGE_BASE_URL=https://api.sandbox.bridge.xyz
BRIDGE_API_PREFIX=/v0
BRIDGE_API_KEY=...
